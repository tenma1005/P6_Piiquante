# P6_Piiquante

Pour commencer, installez node.js côté front et utilisez la commande "npm run start".

Ensuite sur un autre terminal, utilisez la commande "npm init" et "Npm install -g nodemon" et installez les différentes dépendances (dispo sur package.json) côté back.

Et pour finir lancez la commande "nodemon server"
